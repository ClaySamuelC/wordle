import React, {useState} from 'react';

import './WordBox.css';

const WordBox = ({word}) => {
  return (
    <div>
      These are the words you've guessed.
    </div>
  );
};

export default WordBox;
